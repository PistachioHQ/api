# Root options

# The graph table configures how the dependency graph is constructed and thus
# which crates the checks are performed against
[graph]
# If 1 or more target triples (and optionally, target_features) are specified,
# only the specified targets will be checked when running `cargo deny check`.
# This means, if a particular package is only ever used as a target specific
# dependency, such as, for example, the `nix` crate only being used via the
# `target_family = "unix"` configuration, that only having windows targets in
# this list would mean the nix crate, as well as any of its exclusive
# dependencies not shared by any other crates, would be ignored, as the target
# list here is effectively saying which targets you are building for.
targets = [
    # Linux
    "x86_64-unknown-linux-gnu",
    "aarch64-unknown-linux-gnu",
    # Windows
    "x86_64-pc-windows-msvc",
    "aarch64-pc-windows-msvc",
    # macOS
    "x86_64-apple-darwin",
    "aarch64-apple-darwin",
    # iOS
    "aarch64-apple-ios",
    "x86_64-apple-ios",
]
# When creating the dependency graph used as the source of truth when checks are
# executed, this field can be used to prune crates from the graph, removing them
# from the view of cargo-deny. This is an extremely heavy hammer, as if a crate
# is pruned from the graph, all of its dependencies will also be pruned unless
# they are connected to another crate in the graph that hasn't been pruned,
# so it should be used with care. The identifiers are [Package ID Specifications]
# (https://doc.rust-lang.org/cargo/reference/pkgid-spec.html)
#exclude = []
# If true, metadata will be collected with `--all-features`. Note that this can't
# be toggled off if true, if you want to conditionally enable `--all-features` it
# is recommended to pass `--all-features` on the cmd line instead
all-features = false
# If true, metadata will be collected with `--no-default-features`. The same
# caveat with `all-features` applies
no-default-features = false
# If set, these feature will be enabled when collecting metadata. If `--features`
# is specified on the cmd line they will take precedence over this option.
#features = []

# The output table provides options for how/if diagnostics are outputted
[output]
# When outputting inclusion graphs in diagnostics that include features, this
# option can be used to specify the depth at which feature edges will be added.
# This option is included since the graphs can be quite large and the addition
# of features from the crate(s) to all of the graph roots can be far too verbose.
# This option can be overridden via `--feature-depth` on the cmd line
feature-depth = 1

# This section is considered when running `cargo deny check advisories`
# More documentation for the advisories section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/advisories/cfg.html
[advisories]
# The path where the advisory databases are cloned/fetched into
#db-path = "$CARGO_HOME/advisory-dbs"
# The url(s) of the advisory databases to use
#db-urls = ["https://github.com/rustsec/advisory-db"]
# A list of advisory IDs to ignore. Note that ignored advisories will still
# output a note when they are encountered.
ignore = [
]
# If this is true, then cargo deny will use the git executable to fetch advisory database.
# If this is false, then it uses a built-in git library.
# Setting this to true can be helpful if you have special authentication requirements that cargo-deny does not support.
# See Git Authentication for more information about setting up git authentication.
#git-fetch-with-cli = true

# This section is considered when running `cargo deny check licenses`
# More documentation for the licenses section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/licenses/cfg.html
[licenses]
# List of explicitly allowed licenses
# See https://spdx.org/licenses/ for list of possible licenses
# [possible values: any SPDX 3.11 short identifier (+ optional exception)].
allow = [
    # Permissive licenses
    "MIT",
    "Apache-2.0",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    # Community data license for webpki-roots (Mozilla CA certificates)
    "CDLA-Permissive-2.0",
]
# The confidence threshold for detecting a license from license text.
# The higher the value, the more closely the license text must be to the
# canonical license text of a valid SPDX license file.
# [possible values: any between 0.0 and 1.0].
confidence-threshold = 0.8
# Allow 1 or more licenses on a per-crate basis, so that particular licenses
# aren't accepted for every possible crate as with the normal allow list
exceptions = [
    # Each entry is the crate and version constraint, and its specific allow
    # list
    #{ allow = ["Zlib"], crate = "adler32" },
]

# Some crates don't have (easily) machine readable licensing information,
# adding a clarification entry for it allows you to manually specify the
# licensing information
#[[licenses.clarify]]
# The package spec the clarification applies to
#crate = "ring"
# The SPDX expression for the license requirements of the crate
#expression = "MIT AND ISC AND OpenSSL"
# One or more files in the crate's source used as the "source of truth" for
# the license expression. If the contents match, the clarification will be used
# when running the license check, otherwise the clarification will be ignored
# and the crate will be checked normally, which may produce warnings or errors
# depending on the rest of your configuration
#license-files = [
# Each entry is a crate relative path, and the (opaque) hash of its contents
#{ path = "LICENSE", hash = 0xbd0eed23 }
#]

[licenses.private]
# If true, ignores workspace crates that aren't published, or are only
# published to private registries.
# To see how to mark a crate as unpublished (to the official registry),
# visit https://doc.rust-lang.org/cargo/reference/manifest.html#the-publish-field.
ignore = false
# One or more private registries that you might publish crates to, if a crate
# is only published to private registries, and ignore is true, the crate will
# not have its license(s) checked
registries = [
    #"https://sekretz.com/registry
]

# This section is considered when running `cargo deny check bans`.
# More documentation about the 'bans' section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/bans/cfg.html
[bans]
# Lint level for when multiple versions of the same crate are detected
multiple-versions = "warn"
# Lint level for when a crate version requirement is `*`
wildcards = "allow"
# The graph highlighting used when creating dotgraphs for crates
# with multiple versions
# * lowest-version - The path to the lowest versioned duplicate is highlighted
# * simplest-path - The path to the version with the fewest edges is highlighted
# * all - Both lowest-version and simplest-path are used
highlight = "all"
# The default lint level for `default` features for crates that are members of
# the workspace that is being checked. This can be overridden by allowing/denying
# `default` on a crate-by-crate basis if desired.
workspace-default-features = "allow"
# The default lint level for `default` features for external crates that are not
# members of the workspace. This can be overridden by allowing/denying `default`
# on a crate-by-crate basis if desired.
external-default-features = "allow"
# List of crates that are allowed. Use with care!
allow = []

# PERMANENT: p256 (and related RustCrypto elliptic curve crates) require rand_core 0.6
# for the CryptoRngCore trait used in key generation. rand 0.9 uses rand_core 0.9,
# which is incompatible. libgn-crypto-internal explicitly depends on rand_core 0.6
# (as rand_core_06) for these APIs only.
# See: https://github.com/RustCrypto/elliptic-curves/issues/1058
# DO NOT REMOVE these skips until p256 supports rand_core 0.9.
skip = [
    { crate = "getrandom@0.2", reason = "PERMANENT: follows rand_core version split for p256 compatibility" },
    { crate = "rand_core@0.6", reason = "PERMANENT: p256 requires rand_core 0.6 for CryptoRngCore trait" },
]
# Similarly to `skip` allows you to skip certain crates during duplicate
# detection. Unlike skip, it also includes the entire tree of transitive
# dependencies starting at the specified crate, up to a certain depth, which is
# by default infinite.
skip-tree = [
    # Skip windows-sys and all its transitive deps (windows-targets, windows_*)
    # due to version mismatch: ring uses 0.52.x, tokio/socket2 uses 0.60.x
    { crate = "windows-sys", reason = "transitive dependency version mismatch" },
]

# ============================================================================
# Internal crypto implementation - must not be used directly
# ============================================================================
[[bans.deny]]
crate = "libgn-crypto-internal"
reason = "Use libgn-crypto for all cryptographic operations. libgn-crypto-internal is an implementation detail."
wrappers = ["libgn-crypto"]

# ============================================================================
# Cryptography and randomness crates must only be used through libgn-crypto.
# This ensures all crypto operations are centralized and auditable.
# libgn-crypto-internal is allowed as a wrapper since it's the implementation layer.
# ============================================================================

# Symmetric encryption
[[bans.deny]]
crate = "aes"
reason = "Use libgn-crypto for encryption operations."
wrappers = ["libgn-crypto-internal", "aes-gcm", "aes-kw"]

[[bans.deny]]
crate = "aes-gcm"
reason = "Use libgn-crypto for authenticated encryption."
wrappers = ["libgn-crypto-internal"]

[[bans.deny]]
crate = "aes-kw"
reason = "Use libgn-crypto for key wrapping."
wrappers = ["libgn-crypto-internal"]

# Block cipher modes
[[bans.deny]]
crate = "cbc"
reason = "Use libgn-crypto for encryption operations."
wrappers = ["libgn-crypto-internal"]

[[bans.deny]]
crate = "ctr"
reason = "Use libgn-crypto for encryption operations."
wrappers = ["libgn-crypto-internal", "aes-gcm"]

[[bans.deny]]
crate = "cipher"
reason = "Use libgn-crypto for encryption operations."
wrappers = ["libgn-crypto-internal", "aes", "aes-gcm", "cbc", "ctr"]

# Hashing and MACs
[[bans.deny]]
crate = "sha2"
reason = "Use libgn-crypto for hashing operations."
wrappers = ["libgn-crypto-internal", "p256"]

[[bans.deny]]
crate = "hmac"
reason = "Use libgn-crypto for HMAC operations."
wrappers = ["libgn-crypto-internal", "rfc6979"]

[[bans.deny]]
crate = "digest"
reason = "Use libgn-crypto for hashing operations."
wrappers = ["sha2", "hmac", "ecdsa", "elliptic-curve", "signature"]

# Elliptic curve cryptography
[[bans.deny]]
crate = "p256"
reason = "Use libgn-crypto for ECDSA signing and verification."
wrappers = ["libgn-crypto-internal"]

[[bans.deny]]
crate = "ecdsa"
reason = "Use libgn-crypto for ECDSA signing and verification."
wrappers = ["libgn-crypto-internal", "p256"]

[[bans.deny]]
crate = "elliptic-curve"
reason = "Use libgn-crypto for elliptic curve operations."
wrappers = ["p256", "ecdsa", "primeorder"]

# Randomness
[[bans.deny]]
crate = "rand"
reason = "Use libgn-crypto for secure random generation."
wrappers = ["libgn-crypto-internal"]

[[bans.deny]]
crate = "rand_core"
reason = "Use libgn-crypto for secure random generation."
wrappers = ["libgn-crypto-internal", "rand", "rand_chacha", "signature", "group", "ff", "elliptic-curve", "crypto-common", "crypto-bigint"]

[[bans.deny]]
crate = "getrandom"
reason = "Use libgn-crypto for secure random generation."
wrappers = ["rand_core", "ring", "tempfile"]

# Memory security
[[bans.deny]]
crate = "zeroize"
reason = "Use libgn-crypto types which handle secure memory automatically."
wrappers = ["libgn-crypto-internal", "crypto-bigint", "der", "elliptic-curve", "generic-array", "rustls", "rustls-pki-types", "sec1"]

# Encoding (used in crypto contexts)
[[bans.deny]]
crate = "base64ct"
reason = "Use libgn-crypto for constant-time encoding in crypto contexts."
wrappers = ["libgn-crypto-internal", "pem-rfc7468"]

# TLS libraries - only allowed via high-level HTTP/gRPC clients
[[bans.deny]]
crate = "ring"
reason = "Do not use ring directly. Use standard HTTP clients (reqwest, tonic) for TLS."
wrappers = ["rustls", "rustls-webpki"]

[[bans.deny]]
crate = "rustls"
reason = "Do not configure TLS directly. Use standard HTTP clients (reqwest, tonic)."
wrappers = ["hyper-rustls", "reqwest", "tokio-rustls"]

[[bans.deny]]
crate = "rustls-pki-types"
reason = "Do not use TLS types directly. Use standard HTTP clients (reqwest, tonic)."
wrappers = ["hyper-rustls", "reqwest", "rustls", "rustls-webpki", "webpki-roots"]

# ============================================================================
# Transitive crypto dependencies
# These are internal implementation crates that should not be used directly.
# ============================================================================

# Signature infrastructure
[[bans.deny]]
crate = "signature"
wrappers = ["ecdsa"]

[[bans.deny]]
crate = "rfc6979"
wrappers = ["ecdsa"]

# Elliptic curve infrastructure
[[bans.deny]]
crate = "primeorder"
wrappers = ["p256"]

[[bans.deny]]
crate = "sec1"
wrappers = ["elliptic-curve"]

[[bans.deny]]
crate = "ff"
wrappers = ["elliptic-curve", "group"]

[[bans.deny]]
crate = "group"
wrappers = ["elliptic-curve"]

[[bans.deny]]
crate = "crypto-bigint"
wrappers = ["elliptic-curve"]

# Key encoding (PKCS#8, SPKI, DER, PEM)
[[bans.deny]]
crate = "pkcs8"
wrappers = ["elliptic-curve", "sec1"]

[[bans.deny]]
crate = "spki"
wrappers = ["ecdsa", "pkcs8"]

[[bans.deny]]
crate = "der"
wrappers = ["ecdsa", "pkcs8", "sec1", "spki"]

[[bans.deny]]
crate = "pem-rfc7468"
wrappers = ["der", "elliptic-curve"]

# Crypto common infrastructure
[[bans.deny]]
crate = "crypto-common"
wrappers = ["aead", "cipher", "digest", "universal-hash"]

[[bans.deny]]
crate = "generic-array"
wrappers = ["aead", "block-buffer", "block-padding", "crypto-bigint", "crypto-common", "elliptic-curve", "inout", "sec1"]

[[bans.deny]]
crate = "block-buffer"
wrappers = ["digest"]

[[bans.deny]]
crate = "inout"
wrappers = ["cipher"]

[[bans.deny]]
crate = "block-padding"
wrappers = ["inout"]

# AEAD infrastructure
[[bans.deny]]
crate = "aead"
wrappers = ["aes-gcm"]

[[bans.deny]]
crate = "ghash"
wrappers = ["aes-gcm"]

[[bans.deny]]
crate = "polyval"
wrappers = ["ghash"]

[[bans.deny]]
crate = "universal-hash"
wrappers = ["polyval"]

# RNG infrastructure
[[bans.deny]]
crate = "rand_chacha"
wrappers = ["rand"]

# UUID - banned entirely (misuse for entropy/unique identifiers in crypto contexts)
[[bans.deny]]
crate = "uuid"
reason = "UUIDs should not be used for cryptographic entropy or identity. Use libgn-crypto for secure random generation."

# Nanoid - banned entirely (use libgn-crypto's built-in nanoid implementation)
[[bans.deny]]
crate = "nanoid"
reason = "Use libgn-crypto::random::nanoid for ID generation."

# This section is considered when running `cargo deny check sources`.
# More documentation about the 'sources' section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/sources/cfg.html
[sources]
# Lint level for what to happen when a crate from a crate registry that is not
# in the allow list is encountered
unknown-registry = "warn"
# Lint level for what to happen when a crate from a git repository that is not
# in the allow list is encountered
unknown-git = "warn"
# List of URLs for allowed crate registries. Defaults to the crates.io index
# if not specified. If it is specified but empty, no registries are allowed.
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
# List of URLs for allowed Git repositories
allow-git = []

[sources.allow-org]
# github.com organizations to allow git sources for
github = ["PistachioHQ"]
# gitlab.com organizations to allow git sources for
gitlab = []
# bitbucket.org organizations to allow git sources for
bitbucket = []
